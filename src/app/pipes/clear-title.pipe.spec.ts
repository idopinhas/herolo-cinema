import { ClearTitlePipe } from './clear-title.pipe';

describe('ClearTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new ClearTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
